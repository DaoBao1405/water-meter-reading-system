IF OBJECT_ID('dbo.LSTM_SINGLE_LOGGER_ALERT_RESULT', 'U') IS NULL
BEGIN
    CREATE TABLE dbo.LSTM_SINGLE_LOGGER_ALERT_RESULT (
        ID INT IDENTITY(1,1) PRIMARY KEY,
        MALOGER VARCHAR(20),
        PREDICTION_TIME DATETIME,
        PREDICTED_APLUC FLOAT,
        ALERT_TYPE VARCHAR(50),
        ALERT_MESSAGE NVARCHAR(255),
        CREATED_AT DATETIME DEFAULT GETDATE()
    );
END;
